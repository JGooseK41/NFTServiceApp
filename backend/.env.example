# Backend Environment Variables

# Server Configuration
NODE_ENV=development
PORT=3001

# Database Configuration (PostgreSQL)
# For Render: Use the Internal Database URL from your PostgreSQL instance
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Frontend URL (for CORS)
# Update this to your actual frontend URL
FRONTEND_URL=https://nft-legal-service.netlify.app

# Optional: API Keys for enhanced features
# Get your own IP geolocation API key from ipapi.com or similar
IP_API_KEY=your_ip_api_key_here